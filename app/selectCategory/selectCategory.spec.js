'use strict';

describe('myApp.selectCategory module', function() {

  beforeEach(module('myApp.selectCategory'));

  describe('selectCategory controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var selectCategoryCtrl = $controller('selectCategoryCtrl');
      expect(selectCategoryCtrl).toBeDefined();
    }));

  });
});